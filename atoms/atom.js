import { atom } from "jotai";

const selectedMarkerAtom = atom(null);
const selectedMarkerDetailAtom = atom(null);
const mapAtom = atom(null);
const commonCodeAtom = atom(null);

export { selectedMarkerAtom, selectedMarkerDetailAtom, mapAtom, commonCodeAtom };
